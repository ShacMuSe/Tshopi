export interface Order {
    productId: number;
    firstName: string;
    lastName: string;
    phone: string;
    email: string;
    address: string;
    quantity: number;
    selectedImage: String;
}
